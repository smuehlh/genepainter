<%=
content_tag(:div, 
	content_tag(:normal, "In order to plot intron gain and loss events onto the phylogenetic tree (based on NCBI taxonomy), GenePainter requires a mapping between gene names (FASTA headers) and species names.".html_safe
	), 
	:id => "species_mapping_info",
	:class => "info"
)
%>