<%=
content_tag(:div, 
	content_tag(:span, "", 
		:class => "ui-icon ui-icon-closethick warning_close", 
		:style => "display:inline-block;",
		:onclick => "$('#upload_seq_help').hide('slow');"
	) +		
	content_tag(:normal,
		"Upload a multiple protein sequence alignment from your local disk. The alignment must be FASTA-formatted. Protein sequences are matched with the gene structures on the basis of the FASTA header and file names, respectively. For this reason, FASTA headers must not contain any blanks or special characters.".html_safe,
	),
	:id => "upload_seq_help",
	:class => "help",
	:style => "display:none;"
)
%>
