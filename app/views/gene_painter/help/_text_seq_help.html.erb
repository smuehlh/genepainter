<%=
content_tag(:div, 
	content_tag(:span, "", 
		:class => "ui-icon ui-icon-closethick warning_close", 
		:style => "display:inline-block;",
		:onclick => "$('#text_seq_help').hide('slow');"
	) +		
	content_tag(:normal,
		"Enter multiple protein sequence alignment in the text input field. The alignment must be FASTA-formatted. Protein sequences are matched with the gene structures on the basis of the FASTA header and file names, respectively. For this reason, FASTA headers must not contain any blanks or special characters.".html_safe,
	),
	:id => "text_seq_help",
	:class => "help",
	:style => "display:none;"
)
%>
