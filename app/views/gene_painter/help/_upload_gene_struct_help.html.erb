<%=
content_tag(:div, 
	content_tag(:span, "", 
		:class => "ui-icon ui-icon-closethick warning_close", 
		:style => "display:inline-block;",
		:onclick => "$('#upload_gene_struct_help').hide('slow');"
	) +		
	content_tag(:normal, 
		"Gene structures must correspond to an alignment sequence. They must be either in YAML format and generated by <a href='http://www.webscipio.org/' target='_blank' class='external'>WebScipio</a> or in GFF v.3 format. File names must equal the corresponding gene name (FASTA header). If the gene structure is in YAML format and specifies a gene name in the YAML description, this gene name must also equal the file name.".html_safe,
	),
	:id => "upload_gene_struct_help",
	:class => "help",
	:style => "display:none;"
)
%>
